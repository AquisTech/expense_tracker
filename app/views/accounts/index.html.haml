%h1
  Accounts
  = render 'shared/family_switch'
  = link_to 'Add New Account', 'javascript:void(0)', class: 'button primary float-right', data: {open: 'ajax-reveal', url: new_account_path}

%table.striped.hover.table-striped.stack
  %thead
    %tr
      %th Id
      %th Name
      %th Description
      %th Details
      %th Account type
      %th Allowed payment modes
      %th Created at
      %th Updated at
      %th Actions

  %tbody
    - @accounts.each do |account|
      %tr
        %td= account.id
        %td= account.name
        %td= account.description
        %td= account.details
        %td= account.account_type_name
        %td= account.payment_modes.join(', ')
        %td= account.created_at
        %td= account.updated_at
        %td
          .button-group
            = link_to 'Show', 'javascript:void(0)', class: 'button success', data: {open: 'ajax-reveal', url: account_path(account)}
            = link_to 'Edit', 'javascript:void(0)', class: 'button warning', data: {open: 'ajax-reveal', url: edit_account_path(account)}
            = link_to 'Delete', account, method: :delete, data: { confirm: 'Are you sure you want to delete this Account?' }, class: 'button alert'
