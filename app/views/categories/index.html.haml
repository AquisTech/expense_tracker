%h1 Categories
%span= link_to 'Add New Category', new_category_path, class: 'button primary float-right'

%table.striped.hover.table-striped.stack
  %thead
    %tr
      %th Id
      %th Name
      %th Created at
      %th Updated at
      %th Actions

  %tbody
    - @categories.each do |category|
      %tr
        %td= category.id
        %td= category.name
        %td= category.created_at
        %td= category.updated_at
        %td
          .button-group
            = link_to 'Show', category, class: 'button success'
            = link_to 'Edit', edit_category_path(category), class: 'button warning'
            = link_to 'Delete', category, method: :delete, data: { confirm: 'Are you sure you want to delete this Category?' }, class: 'button alert'
