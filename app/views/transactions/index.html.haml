%h1 Transactions
%span= link_to 'Add New Transaction', new_transaction_path, class: 'button primary float-right'

%table.striped.hover.table-striped.stack
  %thead
    %tr
      %th Id
      %th Amount
      %th Description
      %th Transaction purpose
      %th Transfer
      %th Created at
      %th Updated at
      %th Actions

  %tbody
    - @transactions.each do |transaction|
      %tr
        %td= transaction.id
        %td= transaction.amount
        %td= transaction.description
        %td= transaction.transaction_purpose_id
        %td= transaction.transfer_id
        %td= transaction.created_at
        %td= transaction.updated_at
        %td
          .button-group
            = link_to 'Show', transaction, class: 'button success'
            = link_to 'Edit', edit_transaction_path(transaction), class: 'button warning'
            = link_to 'Delete', transaction, method: :delete, data: { confirm: 'Are you sure you want to delete this Transaction?' }, class: 'button alert'
